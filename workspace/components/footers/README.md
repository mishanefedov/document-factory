# workspace/components/footers

Brand-specific footer components. One file per brand variant.

Each footer typically shows company name + contact email + date + confidentiality notice. See `instructions/component-conventions.md`.
