# workspace/components/headers

Brand-specific header components. One file per brand variant (e.g. `company-header.html`, `alternate-header.html` if you run multiple brands).

Each header includes logo + company name + domain + eyebrow line. See `instructions/component-conventions.md` for the comment-header schema every component must include.
